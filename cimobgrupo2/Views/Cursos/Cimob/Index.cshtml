@model IEnumerable<cimobgrupo2.Models.Curso>
@{
    ViewData["Titulo"] = "Lista de Cursos";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<table class="table table-striped" id="tabela-cursos">
    <thead class="thead-inverse">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th class="no-sort"></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
            {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nome)
                </td>
                <td class="text-center">
                    <a asp-action="Editar" asp-route-id="@item.CursoId">Editar</a> |
                    <a asp-action="Eliminar" asp-route-id="@item.CursoId">Eliminar</a>
                </td>
            </tr>
        }
    </tbody>
</table>

<div class="col-md-12 col-lg-12 botoes">
    <button type="button" onclick="location.href='@Url.Action("Criar", "Cursos")'" class="pull-right" id="btnCriar" style="margin-bottom: 20px;">Novo Curso</button>
</div>

<script>
    $(document).ready(function () {
        $('#tabela-cursos').DataTable();
    });
</script>